import { GridAdapter } from '@/shared/utils'

/**
 * Detail 그리드 생성히기 위한 composable
 *
 * @description
 * - Detail GridAdapter 객체를 생성해 준다
 * - 한건씩 생성 가능
 *
 * @return TData[] - 생성된 데이터 리스트
 */
export const useDetailGrid = () => {
  return ref<Record<string, GridAdapter>>()
}